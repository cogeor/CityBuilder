[package]
name = "city_core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Core primitives, traits, and plugin framework for the city builder engine"

[dependencies]
serde = { workspace = true }

[package]
name = "city_engine"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "City builder simulation engine — ECS, tick loop, commands, pathfinding"

[dependencies]
city_core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

{
  "name": "townbuilder",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint packages/ web/",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jsdom": "^28.1.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
